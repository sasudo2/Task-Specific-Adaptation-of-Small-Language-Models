FROM python:3.10-slim

# Create a non-root user for security
RUN useradd -m sandbox
USER sandbox

WORKDIR /app

# Copy the runner into the image
COPY docker_runner.py /app/docker_runner.py

# When the container starts, run the runner
CMD ["python", "/app/docker_runner.py"]